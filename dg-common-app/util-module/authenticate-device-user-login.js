const jwt = require('jsonwebtoken');
const userSessionBusiness = require('../business-module/auth-business-module/auth-session-business');

const userLoginAuthenticator = (req, res, next) => {};

module.exports = {
	userLoginAuthenticator,
};
